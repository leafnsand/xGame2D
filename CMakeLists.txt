cmake_minimum_required(VERSION 2.8)
project(xGame2D)
if(WIN32)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -O2 -Wall -std=c++11")
else()
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -O2 -Wall -fPIC -std=c++11")
endif()
add_subdirectory(src)
if(MINGW)
    add_subdirectory(mingw)
endif()